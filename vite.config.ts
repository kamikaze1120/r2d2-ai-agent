// Build config preset — provides TanStack Start, React, Tailwind, path aliases,
// and Cloudflare/edge bundling. Pass extra config via defineConfig({ vite: { ... } }).
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig();
